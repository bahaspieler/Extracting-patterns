# Extracting Patterns from Text files

A script to analyse specific patterns from texts files and extract those information. After that 
the information is exported into excel files with required format.

## Dependencies
`pandas`

`pypiwin32`

